# DATABASE_SCHEMA.md

## 文档状态

- 项目：小奶瓶 / 宝宝成长记录
- 阶段：数据库设计与最小实现更新
- 日期：2026-06-19
- 当前结论：本地默认使用 SQLite；已实现 MySQL 连接、MySQL schema、迁移脚本和第一方合规埋点表；生产目标为华为云 ECS 上宝塔管理的独立 MySQL

## 已确认事实

1. 本地数据库是第一来源。
2. 第一版需要账号。
3. 第一版需要备份恢复。
4. 服务器需要存照片原图。
5. 数据包含儿童和家庭敏感信息。

## 合理推断

1. 服务端数据库应保存最小备份字段和照片对象元数据。
2. 照片二进制不应进入关系数据库，应进入对象存储。
3. 月报可由客户端生成，不必保存冗余内容。
4. 删除字段和保留周期必须设计在 schema 里。
5. 运营分析只能保存行为枚举和聚合所需字段，不保存宝宝内容。

## 待我确认的问题

1. 华为云 ECS、宝塔 MySQL 版本、正式库名和连接方式。
2. 是否保存照片缩略图对象。
3. 删除后是否有短期恢复窗口。

## 不进入第一版的功能

1. BI 宽表。
2. 用户画像表。
3. 广告归因表。
4. 内容审核队列表。

## 当前最小表

| 表 | 用途 | 状态 |
|---|---|---|
| accounts | 账号、恢复密钥哈希、删除状态 | 已实现 |
| backups | 每个账号最新 JSON 备份 payload | 已实现 |
| photos | 照片对象元数据、大小、hash、更新时间 | 已实现 |
| deletion_audit | 账号删除时间、备份删除和照片删除数量 | 已实现 |
| analytics_events | 第一方白名单行为事件、账号 HMAC、时间和枚举属性 | 已实现 |

## 生产数据库

1. 使用 `XNP_DATABASE_BACKEND=mysql` 切换到 MySQL。
2. 使用 `Backend/scripts/migrate_database.py` 创建生产 schema。
3. MySQL 配置只从私有环境变量读取，不进入仓库或客户端。
4. 真实宝塔 MySQL 上线前必须完成连接、备份、恢复、故障切换和删除验证。
5. 生产库名和账号必须使用小奶瓶命名空间，例如 `xiaonaiping_prod` 与 `xiaonaiping_app`，禁止复用情绪 App / 一根呆毛数据库。

## 为什么先不拆业务明细表

第一版同步目标是备份恢复，不是云端运营查询。把宝宝档案、记录、提醒和照片元数据保存为账号级 JSON 备份，可以减少服务端可查询敏感字段、降低索引泄露面，并保持后端不扩展成管理后台。若未来需要多人协作或冲突合并，再按 `DATA_MODEL.md` 拆分业务表。

## 数据库安全要求

1. 所有表必须按 userId 隔离。
2. 高敏感字段不得写入非必要索引。
3. 备份文件必须有访问控制。
4. 日志和错误上报不得包含敏感字段。
5. 照片对象 key 不得包含宝宝姓名、生日或原始文件名。
6. `analytics_events.account_hash` 必须使用 HMAC，不得保存原始账号 ID。
7. `analytics_events.properties_json` 只能保存白名单枚举，不得保存自由文本、数值明细、照片 key、手机号或微信标识。
8. 账号删除时必须删除对应 `analytics_events` 行。
